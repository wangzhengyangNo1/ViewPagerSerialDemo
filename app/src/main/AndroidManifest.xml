<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.wzhy.viewpagerserial">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".VpsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".transformer.TransformerUsageActivity"
            android:label="PageTransformer实现页面切换效果"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".banner.BannerActivity"
            android:label="ViewPager 轮播图"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".nav.NavActivity"
            android:label="ViewPager 导航联动 带角标"
            android:screenOrientation="portrait"/>

        <activity android:name=".MainActivity" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>